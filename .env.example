# MongoDB Atlas (required)
MONGO_URI=mongodb+srv://USER:PASSWORD@cluster.mongodb.net/prepzo?retryWrites=true&w=majority

# Security (required — use long random strings in production)
JWT_SECRET_KEY=change-me-to-a-long-random-string
FLASK_SECRET_KEY=change-me-to-another-long-random-string

# Optional — used by database/seed_data.py only
ADMIN_EMAIL=admin@prepzo.ai
ADMIN_PASSWORD=Admin@Prepzo2026
